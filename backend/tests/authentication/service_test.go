package authentication

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestCreateUser(t *testing.T) {
	t.Run("Should create an user correctly", func(t *testing.T) {
		assert.Equal(t, true, true)
	})
}
